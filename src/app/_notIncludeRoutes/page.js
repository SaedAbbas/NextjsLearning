import React from 'react'

const page = () => {
  return (
    <div>
      this page is not included in this routing just for code not display in browser like custom hook
      _notincludeRoutes 

        if i needed to merge in url i have to put %5f before the name like %5fincludeRoutes
    
    </div>
  )
}

export default page
